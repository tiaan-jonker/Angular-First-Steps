import { MilesToKmConvertPipe } from './miles-to-km-convert.pipe';

describe('MilesToKmConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new MilesToKmConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
